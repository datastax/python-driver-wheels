import toml

(pyproject_path, libev_includes,libev_libs) = sys.argv[1:]

origin_toml = toml.load(pyproject_path)
origin_toml["tool.cassandra-driver"]["libev-includes] = [libev_includes]
origin_toml["tool.cassandra-driver"]["libev-libs] = [libev_libs]

print toml.dump(origin_toml)

